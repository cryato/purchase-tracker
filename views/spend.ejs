<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
	<title><%= t('spend.title') %></title>
	<link rel="stylesheet" href="/styles.css" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#0f172a">
</head>
<body>
	<div class="container">
		<div class="card">
			<h1 class="title"><%= t('spend.title') %></h1>
			<form action="/spend" method="post">
				<input type="hidden" name="_csrf" value="<%= csrfToken %>">
				<div>
					<label for="amount"><%= t('spend.amount', { currency: currencyCode }) %></label>
					<input id="amount" name="amount" type="number" step="0.01" inputmode="decimal" placeholder="0.00" required />
				</div>
				<div>
					<label for="date"><%= t('spend.date') %></label>
					<input id="date" name="date" type="date" value="<%= defaultDate %>" required />
				</div>
				<div>
					<label for="description"><%= t('spend.description') %></label>
					<textarea id="description" name="description" rows="2" placeholder="<%= t('spend.description_placeholder') %>"></textarea>
				</div>
				<div class="actions">
					<a class="btn secondary" href="/"><%= t('spend.cancel') %></a>
					<button class="btn" type="submit"><%= t('spend.submit') %></button>
				</div>
			</form>
		</div>
	</div>
</body>
</html>
